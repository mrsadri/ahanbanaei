import ahanonline from './ahanonline.mjs'

// Every source listed here is wired into the daily scrape. Add a new one by
// copying _template.mjs, completing the checklist in BUILD-PLAN.md section
// 4.8, recording it in README.md, and adding it to this array.
export default [ahanonline]
